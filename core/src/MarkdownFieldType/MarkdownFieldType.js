import React, { Component } from "react";
import styles from "./MarkdownFieldType.less";

export class MarkdownFieldType extends Component {
  render() {
    return (
      <div className={styles.MarkdownFieldType}>
        write a better component than this
      </div>
    );
  }
}
